const { composeApp } = require('@meenjs/core');

composeApp(
    '{{ appName }}',
    {
        preset: 'api',
    },
    [
        require('./module/ping'),
        require('./module/user'),
    ],
).run();
