module.exports = (app) => {
    const logger = app.logger('controllers/userController');
    const controller = {};

    controller.ping = async (req, res) => {
        res.send('pong');
    };

    controller.pingAuth = async (req, res, next) => {
        res.json({
            status: true,
            message: 'pong',
        });
    };

    return controller;
};
